<Page
    x:Class="WalkaChomika2016.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:WalkaChomika2016"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="80*"/>
            <RowDefinition Height="50*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*"/>
            <ColumnDefinition Width="1*"/>
        </Grid.ColumnDefinitions>

        <StackPanel x:Name="me" Grid.Column="0" Grid.Row="1">
            <TextBlock x:Name="meName" Text="Imię (Typ)" FontSize="30" />
            <TextBlock x:Name="meHP" Text="HP" FontSize="20" />
            <TextBlock x:Name="meDamage" Text="Dmg" FontSize="20" />
        </StackPanel>

        <ScrollViewer Grid.Column="1" Grid.Row="1">
            <ListBox x:Name="inventory" />
        </ScrollViewer>

        <Grid Grid.Column="0" Grid.Row="0" x:Name="location">
            <Grid.RowDefinitions>
                <RowDefinition Height="1*" />
                <RowDefinition Height="8*" />
                <RowDefinition Height="2*" />
            </Grid.RowDefinitions>
            <TextBlock x:Name="txtLocationTitle" Grid.Row="0" />
            <ScrollViewer Grid.Row="1">
                <TextBlock FontSize="30" x:Name="txtLocationDescription" />
            </ScrollViewer>
            <ListBox x:Name="lbLocationEnemies" Grid.Row="2" Foreground="Red" />
        </Grid>

        <Grid Grid.Row="0" Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="8*" />
                <RowDefinition Height="5*" />
            </Grid.RowDefinitions>

            <ScrollViewer Grid.Row="0">
                <TextBlock x:Name="txtLog" FontSize="20" />
            </ScrollViewer>

            <StackPanel Grid.Row="1" Orientation="Vertical">
                <Button Content="North" HorizontalAlignment="Stretch" />
                <Button Content="East" HorizontalAlignment="Stretch" />
                <Button Content="South" HorizontalAlignment="Stretch" />
                <Button Content="West" HorizontalAlignment="Stretch" />
            </StackPanel>
        </Grid>
        
    </Grid>
</Page>
